import { Box } from "@material-ui/core";
import React from "react";
import { useStyles } from "./style";

AccountOders.propTypes = {};

function AccountOders(props) {
  const classes = useStyles();

  return <Box className={classes.root}>AccountOders</Box>;
}

export default AccountOders;
