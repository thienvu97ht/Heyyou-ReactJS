import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  root: {
    paddingTop: "140px",
    marginBottom: "50px",
  },

  headingTitle: {
    fontSize: "40px",
    fontFamily: "SVN-Veneer",
    marginBottom: "15px",
  },
}));
